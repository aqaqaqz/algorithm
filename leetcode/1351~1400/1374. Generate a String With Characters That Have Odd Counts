class Solution {
public:
    string generateTheString(int n) {
        string result = "";
        for(int i=0;i<n-1;i++) result += 'a';
        if(n%2==0) result += 'b';
        else result += 'a';
        return result;
    }
};
